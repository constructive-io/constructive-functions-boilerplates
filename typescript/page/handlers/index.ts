/**
 * What the image serves: this feature's pages, reached at their own paths.
 *
 * The identity — the task, the image name, the route, the channel, and the
 * capabilities this feature is allowed to reach — lives in `handler.json`,
 * because the platform reads it too. This file is behaviour only.
 */
export { pages } from './____method____';
